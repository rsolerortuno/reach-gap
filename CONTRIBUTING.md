# Contributing

Changes to physical parameters require a primary citation, a range and an assumption-ledger update.
Clinical labels may never enter parameter selection or threshold code. Add tests for numerical limits,
abstention and provenance. Run `ruff`, `mypy --strict` and `pytest` before opening a pull request.
